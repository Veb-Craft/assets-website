import React from "react";

const Hero = () => {
  return (
    <div className="relative h-screen w-full bg-black">
      <div className="absolute bottom-0 h-5 w-full rounded-t-full bg-white"></div>
    </div>
  );
};

export default Hero;
